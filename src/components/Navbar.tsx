import CartWidget from "./CartWidget"

const Navbar = () => {
  return (
    <div className="navbar">
        <div>Navbar</div>
        <CartWidget />
    </div>
  )
}

export default Navbar